ALOO SMM PANEL — RAILWAY DEPLOY CHECKLIST (v10.0.0)

1. PostgreSQL service tayyor bo‘lsin.

2. Backend service:
   Root Directory: /backend
   Config Path: /backend/railway.toml

   Majburiy variables:
   NODE_ENV=production
   DATABASE_URL=${{Postgres.DATABASE_URL}}
   JWT_SECRET=<kamida 32 belgilik doimiy maxfiy kalit>
   CORS_ORIGIN=https://FRONTEND-DOMAIN.up.railway.app,https://aloosmm.uz
   ADMIN_FULL_NAME=Aloo Admin
   ADMIN_LOGIN=admin
   ADMIN_PASSWORD=123456

   Ixtiyoriy AI variables:
   OPENAI_API_KEY=
   OPENAI_MODEL=gpt-5-mini

   Ixtiyoriy Web Push variables:
   VAPID_PUBLIC_KEY=
   VAPID_PRIVATE_KEY=
   VAPID_SUBJECT=mailto:admin@aloosmm.uz
   Kalit yaratish: cd backend && npm install && npm run generate:vapid

3. Frontend service:
   Root Directory: /frontend
   Config Path: /frontend/railway.toml
   Variable:
   API_URL=https://BACKEND-DOMAIN.up.railway.app

4. Deploy tartibi:
   PostgreSQL -> Backend -> Frontend

5. Har ikkala servisda:
   Deployments -> Clear build cache and redeploy

6. Tekshirish:
   Backend /health: ok=true
   Backend /ready: initializationComplete=true
   Frontend /health: apiConfigured=true
   Frontend /runtime-config.js: API_URL bo‘sh bo‘lishi normal; browser /api so‘rovlarini frontend proxy orqali yuboradi

7. Login:
   admin / 123456

8. v10 tekshiruvi:
   - Ctrl/Cmd + K menyusini ochish.
   - Kontent -> shablon orqali tezkor kontent yaratish.
   - Kalendar -> kontentni boshqa kunga sudrash.
   - Kontent -> workflow/izoh va Undo sinovi.
   - Media -> faylni drag-and-drop yuklash va crop tanlash.
   - Kontent -> Qo‘shimcha sozlamalar ichidan media coverini bir bosishda biriktirish.
   - AI tugmasi -> lokal matn yaratish.
   - Dark mode va mobil bottom navigationni tekshirish.
   - Brauzer install prompt orqali PWA o‘rnatishni tekshirish.

9. Gilroy:
   Litsenziyalangan font fayllarini frontend/public/fonts/ papkasiga joylashtiring.
   Font fayllari ZIP ichiga kiritilmagan.
